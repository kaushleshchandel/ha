############################################################
# recorder
############################################################
#db_url: sqlite:///data/home-assistant_v2.db
purge_interval: 1
purge_keep_days: 30
include:
  entities:
    - group.landscaping
exclude:
  domains:
    - updater
    - automation
    - device_tracker
    - group
    - scene
    - script
  entities:
    - sun.sun
    - zone.home
    - sensor.outdoor_temperature
    - sensor.outdoor_humidity